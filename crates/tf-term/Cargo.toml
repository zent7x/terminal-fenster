[package]
name = "tf-term"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Terminal capability detection, graphics protocol encoders, and input decoding for Terminal-Fenster"

[dependencies]
libc.workspace = true
flate2.workspace = true

[dev-dependencies]
# Used only by tests/encode_roundtrip.rs to inflate the encoder's zlib payload and prove it
# decodes back to the exact input pixels. Already in the graph as a normal dependency, so this
# adds no new locked crate.
flate2.workspace = true
